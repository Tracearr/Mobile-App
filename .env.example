# Tracearr Mobile Environment Variables
# Copy this file to .env and fill in your values

# Injected into the android config by app.config.js. Needed for local android
# builds; CI reads it from the GOOGLE_MAPS_API_KEY secret instead.
GOOGLE_MAPS_API_KEY=
